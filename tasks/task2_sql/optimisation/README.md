# Optimisation sub-task

This folder will contain:

- `slow_query.sql` — a deliberately slow query (provided by us).
- `explain_analyze.txt` — its `EXPLAIN ANALYZE` output against the seeded database.

Your job: rewrite the query to be faster and explain *why* it's faster, referencing the
plan. Put your rewrite in `../queries/q6_optimised.sql` and your explanation in
`../README.md`.
